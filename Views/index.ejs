<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IFSPORT --> LOGIN </title>
    <link rel="stylesheet" href="../Css/Login.css">
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>

    <main class="login-container">
        <div class="logo-box">
            <div class="logo-icon">
                <img src="../img/img_login.png" alt="Logo" class="logo-img">
            </div>
    
        </div>

        <% if (typeof erro !== 'undefined' && erro) { %>
            <div style="color: #e74c3c; background-color: #fde8e8; border: 1px solid #f5c6cb; padding: 10px; border-radius: 6px; margin-bottom: 15px; text-align: center; font-size: 0.9rem; font-weight: 500;">
                <%= erro %>
            </div>
        <% } %>
        <div class="Box_login">
            <form class="login-form" action="/login" method="POST">

                <div class="input-group">
                    <i data-lucide="user" class="input-icon"></i>
                    <input type="text" name="email" placeholder="Digite seu usuário" required>
                </div>

                <div class="input-group">
                    <i data-lucide="lock" class="input-icon"></i>
                    <input type="password" id="inputSenha" name="senha" placeholder="Digite sua senha" required>
                    <span id="btnToggleEye" style="cursor: pointer; display: inline-flex; align-items: center;">
                        <i data-lucide="eye" class="toggle-eye"></i>
                    </span>
                </div>

                <div class="form-options">
                    <label class="remember-me">
                        <input type="checkbox" name="lembrar">
                        <span>Lembrar de mim</span>
                    </label>
                    <a href="#" class="forgot-pass">Esqueci minha senha</a>
                </div>

                <button type="submit" class="btn-entrar">Entrar</button>

            </form>
        </div>    
        

    </main>

    <script>
        lucide.createIcons();

        document.addEventListener("DOMContentLoaded", () => {
            const inputSenha = document.getElementById("inputSenha");
            const btnToggleEye = document.getElementById("btnToggleEye");

            if (btnToggleEye && inputSenha) {
                btnToggleEye.addEventListener("click", () => {
                    const isPassword = inputSenha.getAttribute("type") === "password";
                    
                    inputSenha.setAttribute("type", isPassword ? "text" : "password");
                    
                    const iconName = isPassword ? "eye-off" : "eye";
                    btnToggleEye.innerHTML = `<i data-lucide="${iconName}" class="toggle-eye"></i>`;
                    
                    lucide.createIcons();
                });
            }
        });
    </script>
</body>
</html>